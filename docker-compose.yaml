services:
  server:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      ADDRESS: ":8080"
    #! CREATE 'falkie.db' BEFORE CREATING CONTAINER
    volumes:
      - "./falkie.db:/opt/falkie/falkie.db"
